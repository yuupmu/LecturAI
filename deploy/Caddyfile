{
	email {$ACME_EMAIL}
}

{$SITE_ADDRESS} {
	encode zstd gzip

	basic_auth {
		{$SITE_USERNAME} {$SITE_PASSWORD_HASH}
	}

	request_body {
		max_size 100MB
	}

	reverse_proxy app:3000 {
		transport http {
			read_timeout 10m
		}
	}
}
